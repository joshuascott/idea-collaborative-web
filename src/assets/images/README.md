# Astro-processed image sources

Import from here for `<Image />` and `Photo.astro` (e.g. Front Range photography).
Files here are hashed and optimized at build time; they are not served at a fixed URL.

Drop 2400px-wide graded JPEGs here when licensed photos are ready.
